import { RestaurantesLayout } from './RestaurantesLayoutClient';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <RestaurantesLayout>{children}</RestaurantesLayout>;
}
